# Public source mirror

`src/` is an importable convenience mirror for inspection and reuse.

The **byte-level scientific authority** for the confirmatory study is `reproduce/frozen_confirmatory/`. The only intentional differences in the top-level mirror are packaging/import-path adjustments and public-path sanitization; the mathematical definitions are unchanged.
