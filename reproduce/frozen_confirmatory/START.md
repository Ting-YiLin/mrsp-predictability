# START

1. Read `CONTRACT.md` completely.
2. Do not redesign the experiment.
3. Do not alter frozen P1/B0/B1 code or cutoffs.
4. Run `go.bat` from this package directory.
5. Ordinary engineering repair is allowed only if it does not change `code/` or `cfg/` scientific content. Record it in `w/repair_log.md`.
6. If any `code/` or `cfg/` file must change, preserve the existing `w` as evidence and rerun from an empty `w`; never mix checkpoints across scientific-code identities.
7. Do not use Sol. Do not start an autonomous research loop.
8. Return only the final Audit ZIP, or the newest Failure ZIP if fail-closed stops the run.
